<h1>edit Review</h1>
<%= form_for :review do |f| %>
  Title : <%= f.text_field :title, value: @review.title %><br />
  Body : <%= f.text_area :body, value: @review.body %><br />
  <%= f.submit %>
<% end %>

